<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fxt.newgb.mapper.app.VehicleBatteryConfigMapper">

    <resultMap id="BatteryConfigVOMap" type="com.fxt.newgb.vo.app.BatteryConfigVO">
        <result column="battery_type" property="batteryType"/>
        <result column="battery_type_name" property="batteryTypeName"/>
        <result column="battery_voltage" property="batteryVoltage"/>
        <result column="scheme_id" property="schemeId"/>
        <result column="scheme_name" property="schemeName"/>
    </resultMap>

    <select id="selectConfigVO" resultMap="BatteryConfigVOMap">
        SELECT
            vbc.battery_type,
            CASE vbc.battery_type
                WHEN 'LITHIUM' THEN '锂电池'
                WHEN 'LEAD_ACID' THEN '铅酸电池'
            END AS battery_type_name,
            vbc.battery_voltage,
            vbc.scheme_id,
            bs.scheme_name AS scheme_name
        FROM vehicle_battery_config vbc
        LEFT JOIN battery_scheme bs ON vbc.scheme_id = bs.id
        WHERE vbc.vehicle_id = #{vehicleId}
          AND vbc.deleted = 0
    </select>

    <resultMap id="SchemeOptionVOMap" type="com.fxt.newgb.vo.common.SchemeOptionVO">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
    </resultMap>

    <select id="selectSchemeOptions" resultMap="SchemeOptionVOMap">
        SELECT
            id,
            scheme_name AS name,
            description
        FROM battery_scheme
        WHERE deleted = 0
          AND status = '1'
        <if test="batteryType != null and batteryType != ''">
          AND battery_type = #{batteryType}
        </if>
        <if test="batteryVoltage != null and batteryVoltage != ''">
          AND CAST(battery_voltage AS VARCHAR) = #{batteryVoltage}
        </if>
        ORDER BY id ASC
    </select>

</mapper>
